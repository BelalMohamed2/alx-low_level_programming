#include "main.h"
/**
 * _abs - for finding the abslute value of the input
 * @i: the input of the function
 * Return: the abslute values
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-1 * i);
	}
}
